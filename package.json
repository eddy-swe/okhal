{
  "name": "okhal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n API,CLIENT -c green,cyan \"node dev-server.js\" \"cd client && npm run dev\"",
    "dev:api": "node dev-server.js",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "lint": "cd client && npm run lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.4"
  }
}
